# Claude Code SDLC Harness

## Session Init

At session start, load in order:
1. Read `docs/SKILL_REGISTRY.md` — skill index, phase order, ACTIVE_TASK section map
2. Read `ACTIVE_TASK.md` — current task state (empty = no active task)
3. If `.claude/context/BE_CONTEXT.md` exists → read it (backend codebase snapshot)
4. If `.claude/context/FE_CONTEXT.md` exists → read it (frontend codebase snapshot)

Context files are generated by `close-task` after each merge. They are the fastest way to understand the codebase without re-scanning source files. Always prefer them over cold file reads when starting a session.

## Meta / DX

- **caveman** ([install](https://github.com/juliusbrussee/caveman)): Reduces output tokens ~75% with full technical accuracy. Install once, active every session.
- **grill-me**: Invoked automatically by `decision-grill` for ADR stress-testing. No manual setup needed.

## How to Use

### Starting a task
```
/capture-requirements
```
Then follow the "Next:" prompt at the end of each skill's output.

### Jumping to a workflow
Pick by task type:
- `/workflow full-sdlc` — new feature, complete lifecycle
- `/workflow bug-fix` — fixing a bug
- `/workflow feature-build` — feature with lighter planning
- `/workflow refactor` — structural improvement, no behavior change

### Resuming mid-task
Read `ACTIVE_TASK.md` — find the last populated section, run the skill that reads it next (see `SKILL_REGISTRY.md`).

## State

All task state lives in `ACTIVE_TASK.md` at repo root. Fixed schema — one section per phase:

```
## Requirement        ← capture-requirements
## Design             ← architecture-design
## ADRs               ← decision-grill
## Risks              ← risk-assessment
## Implementation Log ← code-gen, tdd, refactor
## Test Results       ← test-design, coverage-analysis, verification
## Review Findings    ← code-review, security-audit
## Deploy Checklist   ← deploy-checklist
## Post-Deploy        ← post-deploy
```

`ACTIVE_TASK.md` is reset to empty schema by `close-task` after each merge.

## ACTIVE_TASK.md — Empty Schema Template

When creating `ACTIVE_TASK.md` for a new project, use this exact template:

```markdown
# Active Task

## Requirement
<!-- capture-requirements writes here -->

## Design
<!-- architecture-design writes here -->

## ADRs
<!-- decision-grill writes here -->

## Risks
<!-- risk-assessment writes here -->

## Implementation Log
<!-- tdd writes one entry per criterion here -->

## Test Results
<!-- test-design, coverage-analysis, verification write here -->

## Review Findings
<!-- code-review + security-audit write here -->

## Deploy Checklist
<!-- deploy-checklist writes here -->

## Post-Deploy
<!-- post-deploy writes here -->
```

## Phase Gating

Every skill hard-blocks if its required prior section is missing. Gates enforce:

| From phase | To phase | Gate |
|------------|----------|------|
| Intake | Planning | ## Requirement populated |
| Planning | Implementation | ## Design + ## ADRs populated |
| Implementation | Testing | ## Implementation Log populated, TDD green |
| Testing | Review | ## Test Results PASS verdict |
| Review | Integration | ## Review Findings, all CRITICAL resolved |
| Any | close-task | ## Review Findings populated (post-merge) |

## Skill Pattern

Every skill follows this pattern — read it as: self-inject → guard → work → write → suggest next.

```
1. Read ACTIVE_TASK.md → required section
2. Hard block if empty
3. Self-inject: extract relevant fields into meta-prompt
4. Generate output
5. Write to ACTIVE_TASK.md → own section
6. Tell user: "Next: run [next-skill]"
```

## Task Archival

After merge, run `close-task`. It:
1. Archives full ACTIVE_TASK.md to `task-log/YYYYMMDD-[TYPE]-slug.md`
2. Regenerates `.claude/context/FE_CONTEXT.md` and/or `BE_CONTEXT.md`
3. Resets ACTIVE_TASK.md to empty schema

Type tags: `[FE]` `[BE]` `[FULLSTACK]` `[BUGFIX]` `[REFACTOR]` `[INFRA]` `[DOCS]`

## Forbidden

- Skipping phase order (e.g. `code-gen` before `architecture-design`)
- Skipping `verification` before `code-review`
- Skipping `code-review` or `security-audit` before `deploy-checklist`
- Skipping `close-task` after merge (context files become stale)
- Starting a new task without resetting ACTIVE_TASK.md
- Writing multiple tasks into one ACTIVE_TASK.md

## File Map

```
.claude/
  skills/
    intake/          capture-requirements, init-project
    planning/        architecture-design, decision-grill, risk-assessment
    implementation/  code-gen, tdd, refactor
    testing/         test-design, coverage-analysis, verification
    review/          code-review, security-audit
    integration/     deploy-checklist, post-deploy, close-task
  workflows/         full-sdlc, bug-fix, feature-build, refactor
  context/
    FE_CONTEXT.md    ← auto-generated by close-task (FE snapshot)
    BE_CONTEXT.md    ← auto-generated by close-task (BE snapshot)
docs/
  SKILL_REGISTRY.md  ← skill index (load at session start)
  HARNESS_DESIGN.md
  META_PROMPTING.md
  IMPLEMENTATION_PLAN.md
task-log/            ← YYYYMMDD-[TYPE]-slug.md per completed task
examples/
  python-cli-walkthrough.md
  typescript-api-walkthrough.md
ACTIVE_TASK.md       ← current task state (reset after each close-task)
CLAUDE.md            ← this file
```
